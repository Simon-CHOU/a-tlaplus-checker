SPECIFICATION Spec
\* EndpointCoverage is purely constant-level (no state variables).
\* All verification is done via ASSUME statements at parse time.
\* TLC will evaluate all ASSUME assertions and report any violations.
\* If TLC completes without error, all 10 properties hold.
